import { createTheme } from '@mui/material/styles';

const mainTheme = createTheme({
  
    palette: {
      primary:{
        main:"#fff"
      },
      secondary: {
        main: "#484a04",
      }
    },
  });

  export default mainTheme;